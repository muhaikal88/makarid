import React from 'react';
import { UnifiedLogin } from './UnifiedLogin';

// CompanyLoginPortal is now just a wrapper to UnifiedLogin
export const CompanyLoginPortal = () => {
  return <UnifiedLogin />;
};
